# certificate-generator
A Python desktop application built with Tkinter and Pillow to automate bulk certificate generation. It dynamically maps CSV data, auto-scales photos, and renders crisp typography onto high-resolution templates.
